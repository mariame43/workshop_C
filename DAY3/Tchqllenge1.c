#include <stdio.h>
 void  lasomme(){
  int S , x, y;
     printf("entre x : ");
     scanf ("%d",&x);
        printf("entre y: ");
    scanf("%d",&y);
     S = x + y ;
    printf("la somme est : %d",S); 
}

int main() {
 
lasomme();
    return 0;
}